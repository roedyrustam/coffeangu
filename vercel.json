{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/coffeescore",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/server"
    }
  ]
}
